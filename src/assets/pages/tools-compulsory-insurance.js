(function(){
 const tabs=[...document.querySelectorAll('.tab')],panels=[...document.querySelectorAll('.check-panel')];let active='medical';
 function update(){const p=document.getElementById(active),boxes=[...p.querySelectorAll('input[type=checkbox]')],done=boxes.filter(x=>x.checked).length,total=boxes.length,pc=total?Math.round(done/total*100):0;document.getElementById('done').textContent=done;document.getElementById('total').textContent=total;document.getElementById('bar').style.width=pc+'%';document.getElementById('progressText').textContent=done===total?'文件已全部核對，送件前再確認版本與簽章。':'已完成 '+pc+'%，還有 '+(total-done)+' 項待核對。'}
 tabs.forEach(tab=>tab.addEventListener('click',()=>{active=tab.dataset.tab;tabs.forEach(t=>t.setAttribute('aria-selected',String(t===tab)));panels.forEach(p=>p.classList.toggle('active',p.id===active));update()}));document.querySelectorAll('.check input').forEach(x=>x.addEventListener('change',update));update();
 function calculate(){const d=document.getElementById('accidentDate').value,r=document.getElementById('result');if(!d){r.innerHTML='<strong>請先選擇事故日期</strong><span>額度依事故發生日判斷。</span>';return}const newer=d>='2026-07-01';r.innerHTML=newer?'<strong>新制：合計最高 320 萬元</strong><span>醫療最高 20 萬；死亡或第一級失能最高 300 萬。</span>':'<strong>舊制：合計最高 220 萬元</strong><span>醫療最高 20 萬；死亡或第一級失能最高 200 萬。</span>'}document.getElementById('calc').addEventListener('click',calculate);
 const cb=document.getElementById('copyBtn');cb.addEventListener('click',async()=>{const text=document.getElementById('scriptText').textContent;try{await navigator.clipboard.writeText(text);cb.textContent='已複製';setTimeout(()=>cb.textContent='複製話術',1600)}catch(e){const range=document.createRange();range.selectNode(document.getElementById('scriptText'));getSelection().removeAllRanges();getSelection().addRange(range);cb.textContent='請手動複製'}});
 const els=document.querySelectorAll('.reveal');if('IntersectionObserver'in window){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.08});els.forEach(el=>io.observe(el))}else els.forEach(el=>el.classList.add('in'));
})();